# @package _global_
defaults:
  - elda_reference
datamodule:
  root: ${oc.env:ELDA_R1_DATA_ROOT}
  dataset_names: ELDA_R1_NO_BOUNDARY_IDENTITY
  tokenizer_type: source_net_r1
  serializer_version: elda_r1_no_boundary_identity_v1
  cell_mapping_path: ${datamodule.root}/mapping.txt
elda_safety:
  tokenizer_version: elda_r1_no_boundary_identity_v1
logs:
  prefix: logs/train/ELDA_R1_NO_BOUNDARY_IDENTITY/${model.model_name}/${seed}
